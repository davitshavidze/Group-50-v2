#include <iostream>
#include <vectors>
using namespace std

int main static void(String: str[]){
  public void {
    std >> cin >> "Hello dear user!";
    return 0;
  }

  private void {
    cout << "Hello world!";
  }

  private static void method {
    for (let i = 0; names.len; i += names[1]){
      if (name.x = names.x[-1]){
        return name;
      } else {
        return x;
      }
    }

     return 0;
  }
}